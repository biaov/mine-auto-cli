`2026-06-26`

- 🔥 新增命令 `auto ai config` 用来初始化 claude code 权限配置
- ⚙️ 项目 node 版本升级为 `>=24.18.0`
- ⚙️ 更新依赖
- ⚙️ 依赖 `commander` 由 `14.x` -> `15.x`
- ⚙️ 依赖 `inquirer` 由 `13.x` -> `14.x`
- ⚙️ 依赖 `js-yaml` 由 `4.x` -> `5.x`
- ⚙️ 依赖 `pacote` 由 `21.x` -> `22.x`
- ⚙️ 依赖 `@types/node` 由 `25.x` -> `26.x`
- ⚙️ 依赖 `typescript` 由 `5.x` -> `6.x`
- ⚙️ 依赖 `vite` 由 `7.x` -> `8.x`
